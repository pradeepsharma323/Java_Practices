package collection_practice;

import java.io.*;
import java.time.LocalDateTime;
import java.time.ZonedDateTime;
import java.util.LinkedHashSet;
import java.util.Optional;
import java.util.Set;

public class IKMTest2 {
    public static void main(String[] args) {
        /*Set<String> s = new LinkedHashSet<>();
        s.add("3");
        s.add("1");
        s.add("3");
        s.removeIf(s1->s1.contains("1"));
        s.add("2");
        s.remove("2");
        s.forEach(s2-> System.out.println(s+"-"));*/
        /*String a = 1<2? "One": "Two";
        System.out.println(a=="One");*/
        //Optional<String> f = Optional.of("file.java");
        /*File f1 = new File(f.get());
        FileOutputStream fileInputStream = new FileOutputStream(f1);
        Reader r = new FileReader(f1);
        RandomAccessFile randomAccessFile = new RandomAccessFile(f1);
        InputStream iout = new FileInputStream(f1);*/
        //InputStream iout = new InputStream(f1);
    }
}
